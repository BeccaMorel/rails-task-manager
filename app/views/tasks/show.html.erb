<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><strong><%= @task.completed ? "Done !" : "Do it !"%></strong></p>

<p><%= link_to 'Update', edit_task_path %></p>
<p><%= link_to 'Remove', task_path(@task), method: :delete %></p>
<p><%= link_to 'Back', tasks_path %></p>